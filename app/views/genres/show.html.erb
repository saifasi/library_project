<h1><%= @genre.name %></h1>
<p><%= @genre.description %></p>

<h3>Books in <%= @genre.name %>:</h3>
<ul>
  <% @books.each do |book| %>
    <li><%= link_to book.title, book_path(book) %></li>
  <% end %>
</ul>

<%= paginate @books %>
